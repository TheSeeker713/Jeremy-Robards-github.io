{
  "name": "bullet-note-taker",
  "version": "1.0.0",
  "description": "Bullet Note Taker - local-first note app",
  "private": true,
  "scripts": {
    "dev": "live-server --port=8080 --open=./index.html --quiet",
    "start": "npm run dev"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
