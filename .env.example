# Cloudflare Configuration
# DO NOT COMMIT - Copy this to .env and fill in your values

# ===== MAIN SITE DEPLOYMENT (wrangler) =====
# Required: Your Cloudflare Account ID
# Find it at: https://dash.cloudflare.com/ (in the URL after dash.cloudflare.com/)
CLOUDFLARE_ACCOUNT_ID=

# Optional: API Token for headless/CI authentication
# Alternative to interactive `wrangler login`
# Create at: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Account → Cloudflare Pages → Edit
CLOUDFLARE_API_TOKEN=

# Optional: Throttle concurrent uploads for large sites
# Uncomment and set to 4 if deployment stalls or times out on Windows
# CLOUDFLARE_API_MAX_CONCURRENCY=4

# ===== CMS ARTICLE DEPLOYMENT (cms/publish.js) =====
# Cloudflare Account ID (can reuse CLOUDFLARE_ACCOUNT_ID)
CF_ACCOUNT_ID=

# Cloudflare API Token (can reuse CLOUDFLARE_API_TOKEN)
CF_API_TOKEN=

# Cloudflare Pages Project Name for articles (separate from main site)
CF_PAGES_PROJECT=jr-articles

# ===== SITE CONFIGURATION =====
# Optional: Site base URL for canonical links and Open Graph meta tags
SITE_BASE_URL=https://www.jeremyrobards.com
